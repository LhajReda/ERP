# ═══════════════════════════════════════════════════════════════
# FLA7A ERP - Variables d'environnement
# Copier ce fichier en .env et remplir les valeurs
# ═══════════════════════════════════════════════════════════════

# ──────────────── Database (PostgreSQL + PostGIS) ────────────────
DATABASE_URL=postgresql://fla7a_user:password@localhost:5432/fla7a
DB_PASSWORD=your_secure_password

# ──────────────── Redis ────────────────
REDIS_URL=redis://:password@localhost:6379
REDIS_PASSWORD=your_redis_password

# ──────────────── JWT ────────────────
JWT_SECRET=your_jwt_secret_min_64_chars_change_this_in_production
JWT_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d

# ──────────────── MinIO (Object Storage) ────────────────
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_USER=fla7a_minio
MINIO_PASSWORD=your_minio_password
MINIO_BUCKET=fla7a-files

# ──────────────── Meilisearch ────────────────
MEILI_URL=http://localhost:7700
MEILI_KEY=your_meili_key

# ──────────────── LLM (Claude API) - Systeme Agents IA ────────────────
ANTHROPIC_API_KEY=sk-ant-...
LLM_MODEL=claude-sonnet-4-20250514
LLM_MAX_TOKENS=2048
LLM_TEMPERATURE=0.3

# ──────────────── Speech (Whisper - Chat vocal Darija) ────────────────
OPENAI_API_KEY=sk-...
# OU Whisper local :
WHISPER_LOCAL_URL=http://whisper:8080

# ──────────────── Weather API ────────────────
OPENWEATHER_API_KEY=your_key

# ──────────────── Vector Store (RAG) ────────────────
PGVECTOR_ENABLED=true
EMBEDDING_MODEL=text-embedding-3-small

# ──────────────── Agent Settings ────────────────
AGENT_MAX_TOOL_CALLS=5
AGENT_MEMORY_TTL=3600
AGENT_CONVERSATION_LIMIT=50

# ──────────────── Email (SMTP) ────────────────
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email
SMTP_PASS=your_password

# ──────────────── SMS (Twilio) ────────────────
SMS_PROVIDER=twilio
TWILIO_SID=your_sid
TWILIO_TOKEN=your_token
TWILIO_FROM=+212XXXXXXXXX

# ──────────────── App ────────────────
APP_URL=https://fla7a.ma
API_URL=https://api.fla7a.ma
NEXT_PUBLIC_API_URL=http://localhost:4000
NODE_ENV=development
PORT=4000
